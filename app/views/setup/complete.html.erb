<% content_for(:title) { 'Appliance Setup' } %>

    <div class="row center mt-3 justify-content-center">
      <div class="col-6">
        <%= form_for(:setup_confirm, url: setup_confirm_path, html: { id: 'setup_confirm', class: 'styled-form', style: 'width: 100%'}) do |f| %>
          <div class="row center">
            <div class="col">
              <div class="row">
                <h2>Setup Complete</h2>
              </div>
            </div>
	  </div>
	  <hr>
          <div class="row mb-1 center">
		  <h6>Your appliance will now reconfigure and then reboot. This may take a while. <br><br> Once the appliance has rebooted, you can then access it via the external IP address configured.</h5>
          </div>
	<% end %>
      </div>
    </div>
